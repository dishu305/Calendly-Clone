# Example .env file for Calendly-Clone

# Use a real Postgres connection string to enable database storage.
# Leave this placeholder value if you want the local JSON fallback.
DATABASE_URL=your_database_url_here

# Port for the application
PORT=5000

# Optional:
# Set PGSSLMODE=disable for local non-SSL Postgres instances.
# PGSSLMODE=disable
